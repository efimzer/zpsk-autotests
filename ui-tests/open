#!/usr/bin/env bash

set -euo pipefail

REPORT_DIR="${REPORT_DIR:-allure-report}"

if [ ! -d "$REPORT_DIR" ]; then
  echo "Allure report directory '$REPORT_DIR' not found. Run ./ui first." >&2
  exit 1
fi

npx allure open "$REPORT_DIR"
